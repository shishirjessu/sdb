cc_binary(
    name = "sdb",
    srcs = ["sdb.cpp"],
    deps = ["//src:libsdb", "@libedit//:libedit"],  # links to the library defined in src/BUILD
    visibility = ["//visibility:public"],
    copts = ["-std=c++20"],  # optional, to match the same standard
)
